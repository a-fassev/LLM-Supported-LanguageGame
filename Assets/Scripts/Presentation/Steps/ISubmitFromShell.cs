namespace LanguageGame.Presentation.Steps
{
    /// <summary>
    /// Invoked when the quest shell primary action is pressed for a <b>task</b> step (shell «Controlla» button).
    /// </summary>
    public interface ISubmitFromShell
    {
        void SubmitFromShell();
    }
}
