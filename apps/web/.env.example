# Supabase (project settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Server-only Secret API key (sb_secret_...). Bypasses RLS — never NEXT_PUBLIC_, never commit, never ship to Unity.
# If leaked: rotate in dashboard (Secret API keys) immediately.
SUPABASE_SECRET_KEY=your_sb_secret_key

# Optional: only if browser/client code calls Supabase directly (safe to expose).
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Optional: comma-separated origins for CORS (default * for local dev)
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# --- FreitextLlm scorer (LangChain ChatOpenAI -> NVIDIA-hosted OpenAI-compatible endpoint) ---
# Never ship secrets to Unity clients; configure only inside apps/web/.env.local during development.
# NVIDIA_API_KEY=nvidia_pat_or_api_key_here
# NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_EVAL_MODEL=meta/llama-3.x-???-instruct
# LLM_TASK_TIMEOUT_MS=45000
# LLM_TASK_MAX_RETRIES=2
# FREITEXT_LLM_GATE_TTL_MINUTES=25
